global var x:
_L: z != 1

local var z:

local var t:

local var secret:

_Gamma_0: z -> LOW, x -> LOW, t -> LOW, secret -> HIGH
_Rely: (Gamma_x => Gamma_x') && (z == z')
_Guar: TRUE

while (CAS(z, 0, 1) == 0) {
	while (z != 0) {}
}

x = secret;
// x = 0;
// z = 0;
