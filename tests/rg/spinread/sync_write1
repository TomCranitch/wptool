global var x:
_L: z != 1

global var z:
_L: TRUE

local var t:

global var secret:
_L: FALSE

_Gamma_0: z -> LOW, x -> LOW, t -> LOW, secret -> HIGH
_Rely: (Gamma_x => Gamma_x') && (z == 1 => z == z')
_Guar: TRUE // (z == 2 => (z' == z && x' == x))

// TODO should this be while(CAS)

z = 1;
x = secret;
x = 0;
z = 0;
